# The AeoKit API root. Include /api if your deployment mounts routes there.
VITE_AEOKIT_API_URL=/api

# Optional for a fixed hosted deployment. Users can also set a token in the UI.
VITE_AEOKIT_API_TOKEN=
